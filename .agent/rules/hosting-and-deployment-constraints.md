---
trigger: always_on
---

The site must be fully compatible with GitHub Pages.

Only static assets are permitted:

HTML

CSS

Images (PNG, JPG, SVG)

Do not introduce:

Build steps

Package managers

Frameworks (React, Vue, Svelte, etc.)

Server-side logic

All files must render correctly when opened directly from disk or served via GitHub Pages.